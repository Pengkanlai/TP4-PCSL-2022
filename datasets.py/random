import numpy as np
import torch

d = 1000
n = 20

covariance_V = torch.cat((torch.ones(10),torch.ones(d-10)*0.1))

x_train = torch.randn((n,d))*covariance_V
x_test = torch.randn((n,d))*covariance_V

beta = torch.zeros(d)
beta[0] = 1
  
labels_train = beta@x_train.T
labels_test = beta@x_test.T
